# Server-only OpenAI-compatible provider configuration.
# The application defaults to NVIDIA NIM when these values are blank.
AI_BASE_URL=
AI_MODEL=
AI_API_KEY=

# Set to true for deterministic local simulation fixtures.
USE_FIXTURES=
