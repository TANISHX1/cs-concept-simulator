{
  "title": "Bubble Sort: One Pass",
  "visualType": "array",
  "initialState": [5, 2, 4, 1],
  "steps": [
    { "type": "COMPARE", "indices": [0, 1] },
    { "type": "SWAP", "indices": [0, 1] },
    { "type": "COMPARE", "indices": [1, 2] },
    { "type": "SWAP", "indices": [1, 2] },
    { "type": "HIGHLIGHT", "indices": [2, 3] }
  ],
  "pseudocode": "for (int i = 0; i < n - 1; i++) {\n  if (arr[i] > arr[i + 1]) {\n    swap(arr[i], arr[i + 1]);\n  }\n}",
  "complexity": {
    "time": "O(n^2)",
    "space": "O(1)"
  },
  "pitfalls": [
    "Stopping after one pass does not sort the entire array.",
    "Adjacent comparisons must use valid indices."
  ]
}
